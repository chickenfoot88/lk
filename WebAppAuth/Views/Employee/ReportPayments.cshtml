@{
  Layout = "~/Views/Employee/_Layout.cshtml";
  ViewBag.Title = "Оплаты";
}

<section class="wrapper">

  <section class="content-header">
    <div class="content-title">
      <div class="container">
        <div class="row">
          <div class="col-md-12"><h1>@ViewBag.Title</h1></div>
        </div>
      </div>
    </div>
  </section>

  @{
    Html.RenderAction("SetReportListOfPayments", "Report", new { reservation = ViewBag.Reservation });
  }
